STM32 Discovery kit development board with STM32F303 MCU
========================================================

## Flashing the kernel

The kernel can be programmed using OpenOCD. `cd` into `boards/npcm400f`
directory and run:

```bash
$ make flash

(or)

$ make flash-debug
```
